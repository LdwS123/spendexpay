# @stripe/react-stripe-js has not yet declared React 19 in its peer deps,
# but it works fine at runtime. Use legacy-peer-deps to allow installation
# until Stripe publishes an updated peerDependencies field.
legacy-peer-deps=true
